#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef pair<int, int> ii;

void solve()
{
    int l, r;
    scanf("%d %d", &l, &r);

    printf("%d %d\n", l, 2 * l);
}

int main()
{
    int q;
    scanf("%d", &q);
    while (q--)
        solve();

    return 0;
}
