#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef pair<int, int> ii;

int main()
{
    int ncase;
    scanf("%d", &ncase);

    while (ncase--) {
        int num;
        scanf("%d", &num);

        int a, b;
        a = 2;
        b = num - 1;

        printf("%d %d\n", a, b);
    }

    return 0;
}
