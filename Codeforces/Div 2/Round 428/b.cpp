#include <bits/stdc++.h>

using namespace std;

int main()
{
	int n, k;
	scanf("%d %d", &n, &k);

	int a = n * 2, b = n * 2;
	for(int i = 0; i < k; i++) {
		int num;
		scanf("%d", &num);

		int four = num / 4;
		int rem = num % 4;


	}

	return 0;
}
